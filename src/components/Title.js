import React, {Component} from 'react'

const Title = ({text}) =>(
    <div>
        <font size="+4" color="#0000A0">
        <strong>{text}</strong>
        </font>
    </div>
)

export default Title